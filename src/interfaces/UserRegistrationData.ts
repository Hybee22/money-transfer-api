export interface UserRegistrationData {
  username: string;
  password: string;
  // Add any other fields you need for user registration, such as:
  // email?: string;
  // firstName?: string;
  // lastName?: string;
}
